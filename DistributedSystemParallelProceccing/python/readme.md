to run rmi_server, first run this command:
```bash
python -m Pyro5.nameserver
```
but the package Pyro5 should be installed on system/environment.\
then run server on another terminal and client on 3rd terminal.